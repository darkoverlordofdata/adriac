{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "cwd": "${workspaceRoot}/install/src",
            "program": "${workspaceRoot}/install/src/${workspaceRootFolderName}",
            "args": []
        }
    ]
}